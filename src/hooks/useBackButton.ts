// Legacy hook — no longer needed with React Router navigation.
// Kept as a no-op to avoid import errors during migration cleanup.

export function useBackButton() {
  // No-op: browser back button is handled natively by React Router
}
